# Most of these are also included by pip, but installing the system
# package first improves performance (in pymongo's case) and
# installation time (if something would otherwise need compiling)

python3-yaml
python3-tenacity
python3-pymongo
python3-pymongo-ext
